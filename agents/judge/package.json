{
  "name": "@quorum/judge",
  "version": "0.3.0",
  "description": "QUORUM judge agent — AXL recv poll + feature extraction + verdict emission",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"no judge-specific unit tests yet (TypeScript types validate at build time)\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
