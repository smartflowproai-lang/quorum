{
  "name": "@quorum/verifier",
  "version": "0.1.0",
  "description": "QUORUM verifier agent — validates Judge verdicts against on-chain reality, issues ERC-8004-shaped attestations",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test verifier.test.ts"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
